/// <reference types="chrome" />

// Minimal Chrome Extension typings for MV3 runtime.
// Prefer installing @types/chrome for full coverage; this keeps tsc happy in strict setups.
declare const chrome: typeof globalThis.chrome;

